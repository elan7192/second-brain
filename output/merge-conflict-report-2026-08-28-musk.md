---
id: ship:merge-conflict-report-2026-08-28-musk
type: ship
tags:
  - ship
created: 2026-08-28
updated: 2026-08-28
---

# Merge conflict report · musk-algorithm vs main

Fetched `origin/main` at `4bd2ee0`, then merged into `cursor/musk-algorithm-vault-d0be`.

## Simple (fixed)

Catalog split kept. New main ops pages stay on the short [[index]] door. New papers stay in [[index-papers]]. Seventeen new sources prepended to [[index-sources]].

Unions: `AGENTS.md` layout rows, [[MEMORY]] D10/D11/ontology/clip facts, [[Home]] people colors, README CLI, [[how-it-works]], [[llm-wiki]], [[agent-operating-system]] layers plus musk operating order, [[raptor-dispatch]] grok-bot line, [[log]] both appends, [[graph]] mermaid for briefs that still exist, `tools/lint-wiki.py` HUBS.

`wiki/first-principles.md` was modify/delete. Main only added `id:`. Content was a duplicate of musk step 1. Delete stands. Links dropped.

C15 on this branch (CLAUDE.md copy vs D2) remapped to **C39**. Main C15 stays Foundry 3D vs Palantir Foundry.

## ID collisions (remapped, both contents kept)

This branch locked D9 (short index as query path). Main already locked D9 (markdown canonical, FTS disposable).

- Branch D9 catalog split → **D12**. Live query stays D9 `python3 tools/sb ask`.
- The query-path half of that D9 does not compose. See C37.

## Complicated (not silently picked)

**C37. Musk index-as-query vs D9 sb ask.** This branch treated the short [[index]] as the query path. Main already locked live query as `python3 tools/sb ask` and said do not walk [[index]]. This merge does not reopen D9. D12 keeps the catalog split only. Human should say whether the short index should replace `sb ask`.

**C38. Standing ingest brief vs musk skip.** Main ingest step 9 writes a three-sentence `output/` brief. The musk pass deleted that standing brief. Frozen: do not add a new brief, do not delete existing briefs, wait for lan E.

C17 / C18 / C25 arrived from main. Untouched.
