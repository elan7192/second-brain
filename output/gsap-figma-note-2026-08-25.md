---
type: output
tags:
  - growth
created: 2026-08-25
---

# Skills used, Figma not required

Figma is disconnected (`needsAuth`). The DeRonin clip is a 3D WebGL orbit graph, so the live page is `output/growthos-graph.html`, not a 2D mock. Auth Figma later if you want a static chrome board.

Vault pages used: [[growth-operator]], [[context-graph]], [[anti-slop]], [[src-deronin-growthos-vault]]. GSAP core + performance: HUD entrance and sidebar use `autoAlpha` / `xPercent`; overlay pills and orbs use compositor `transform`, not `left`/`top`.
