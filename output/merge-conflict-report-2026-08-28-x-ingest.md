---
id: ship:merge-conflict-report-2026-08-28-x-ingest
type: ship
tags:
  - ship
created: 2026-08-28
updated: 2026-08-28
---

# Merge conflict report · X ingest vs main

Fetched `origin/main` at `923f0f0`, then merged into `cursor/ingest-4ndrearossetti-openconnector-068c`.

## Simple (fixed)

Unions kept main's new pages and this branch's four X ingests. Short [[index]] door stays (D12). Four sources prepended to [[index-sources]]. Concepts [[secret-gateway]], [[flat-context]], and [[project-skill-stack]] added to the Concepts table.

Related links restored on [[agent-operating-system]], [[anti-slop]], [[harness-routing]], [[verifiable-instructions]], [[tokens-as-capital]], [[llm-wiki]], [[memory-engineering]], [[context-graph]], [[entropy-gate]], [[hunt-ship-loop]], [[audited-task-contract]], [[graph]], and [[MEMORY]].

C6 note added: [[src-voxyz-writing-system]] is not the eight missing verifiable-instruction blocks. C6 stays open.

## ID collisions (remapped, both contents kept)

This branch used C14–C17 for the four ingests. Main already uses those codes.

- Branch C14 (browser inject vs API catalog) → **C40**. Main C14 stays eptwts $100k / 10 months.
- Branch C15 (TrueForge 2.7x) → **C41**. Main C15 stays Foundry 3D vs Palantir Foundry.
- Branch C16 (Claude Projects vs vault) → **C42**. Main C16 stays Headlong one stream vs entropy gate.
- Branch C17 (three writing systems) → **C43**. Main C17 stays two claim registries.

## Complicated (not silently picked)

**C17. Two claim registries.** `wiki/data/claims.yaml` vs `wiki/claims.csv`. Untouched.

**C18. Two retrieve engines.** Live query is `python3 tools/sb ask`. `tools/retrieve.py` stays parked. Untouched.

**C25. Retrieval paste vs D9 FTS.** Untouched.

**C37. Musk short index as query path vs D9 `sb ask`.** This merge does not reopen D9. Catalog split stays D12. Human names whether the short index replaces `sb ask`.

**C38. Standing ingest brief vs musk skip.** Frozen: do not add a new standing-brief rule; do not delete existing briefs; wait for lan E. This file is a merge-conflict report, not a new standing-brief rule.
